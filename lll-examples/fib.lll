func fib(n: i64) -> i64 {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

n := 40;
printf("first %d fibonacci numbers!\n", n);
for i in 0..40 {
    printf("%d: %lld\n", i, fib(i));
}
